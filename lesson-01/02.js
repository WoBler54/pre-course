let randomNumber = Math.floor(Math.random() * 100);

randomNumber % 2 == 0 ? console.log(randomNumber , `«${randomNumber} :Это четное число»` ) : console.log( `«${randomNumber} :Это нечетное число»`) ;
